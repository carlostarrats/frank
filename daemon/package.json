{
  "name": "frank-daemon",
  "version": "1.0.0",
  "description": "Frank daemon — Claude Code hooks integration and panel server",
  "type": "module",
  "bin": {
    "frank": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node --import tsx/esm src/cli.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
